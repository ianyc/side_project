題目:
  基於作業二的留言板，改成可以顯示多人留言的留言板，且每個人頭像顏色不同 (自行定義N種顏色輪循)。

說明:
  基於作業二使用過的功能外，額外會使用到的功能:
    1. spread operator (非常重要的觀念):
      e.g.
        a = [1, 2, 3]
        b = [...a, 4, 5, 6]
    2. Array map function (非常重要的觀念):
      e.g.
        a = [1, 2, 3]
        a.map((item, index) => {})

提示:
  1. index.jsx中定義的const [~~~] = useState()改成const [~~~] = useState([])
